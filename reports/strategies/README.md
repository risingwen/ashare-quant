# Strategy Notes

Put strategy-specific buy/sell plan notes here.

Recommended structure:

- one markdown file per strategy
- include: entry rules, exit rules, position sizing, risk limits

Core strategy YAML files are still maintained under `config/strategies/` and are also published.
