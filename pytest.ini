[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: tests that validate multiple modules or external services
    network: tests that call external data providers such as AkShare or Eastmoney
